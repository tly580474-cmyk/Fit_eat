# Bug 修复日志

**日期时间**: 2026-05-23 22:24

## Bug 描述
1. 食材显示格式错误：显示为纯字符串而非 `{name, amount}` 格式
2. 制作步骤中出现 Markdown 语法乱码（链接、加粗、图片等）

## 修复内容

### 1. 修改 `轻饮食软件后端/import_howtocook.py`

添加两个辅助函数：

```python
def parse_ingredient(text):
    """解析食材字符串为 {name, amount} 格式"""
    # 支持多种格式：
    # - "食材名 = 数量"
    # - "食材名 数量+单位"
    # - "食材名，描述，数量"

def clean_markdown(text):
    """清除文本中的 Markdown 语法"""
    # 清除图片 ![alt](url)
    # 清除链接 [text](url) -> text
    # 清除加粗 **text** -> text
    # 清除斜体 *text* -> text
```

修改 `parse_recipe` 函数：
- 食材提取后使用 `parse_ingredient()` 转换格式
- 步骤提取后使用 `clean_markdown()` 清除 Markdown 语法

### 2. 更新数据库
执行脚本更新 `instance/light_diet.db` 中已存在的记录：
- 成功更新 306 条记录
- 食材格式已转换为 `{name, amount}` 格式
- 步骤中的 Markdown 语法已清除

## 修复结果
- 步骤中仍包含 Markdown 语法的记录数: 0
- 已转换为正确格式的食材记录数: 292
- 新导入的食谱将自动使用正确的数据格式
