# Bug 修复日志

**日期时间**: 2026-05-23 22:20

## Bug 描述
食谱介绍中显示原始 Markdown 图片语法，如 `![微波炉鸡蛋羹](./微波炉鸡蛋羹.jpg)`，而不是正常文本。

## 修复内容

### 1. 修改 `轻饮食软件后端/import_howtocook.py`
在 `parse_recipe` 函数中添加清除 Markdown 图片语法的处理：

```python
# 去掉 Markdown 图片语法 ![alt](url)
desc = re.sub(r'!\[.*?\]\(.*?\)', '', desc)
```

### 2. 清理数据库中的脏数据
执行脚本清理 `instance/light_diet.db` 中 116 条包含 Markdown 图片语法的食谱记录：
- 使用正则表达式 `!\[.*?\]\(.*?\)` 匹配并移除所有 Markdown 图片语法
- 对清理后描述为空的记录设置默认描述

## 修复结果
- 成功清理 116 条记录
- 剩余包含 Markdown 图片语法的记录: 0
- 新导入的食谱将自动过滤 Markdown 图片语法
