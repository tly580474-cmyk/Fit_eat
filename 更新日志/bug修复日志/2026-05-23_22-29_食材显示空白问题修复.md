# Bug 修复日志

**日期时间**: 2026-05-23 22:29

## Bug 描述
食材显示为空白，无法显示任何内容。

## 修复内容

### 修改 `轻饮食软件前端/pages/food-detail.html`

1. **添加调试日志**：
   ```javascript
   console.log('Food data:', food);
   console.log('Ingredients:', food.ingredients);
   ```

2. **修改食材渲染逻辑**：
   - 即使 `food.ingredients` 为空，也更新 DOM 显示 "暂无食材信息"
   - 确保无论数据如何，都会更新 DOM

3. **改进空值处理**：
   ```javascript
   const amount = typeof item === 'object' && item.amount ? item.amount : '';
   ```

## 测试建议
1. 刷新页面（Ctrl+F5 强制刷新）
2. 打开浏览器控制台（F12）
3. 查看控制台输出的 `Food data` 和 `Ingredients` 数据
4. 检查后端返回的 ingredients 数据格式是否正确
