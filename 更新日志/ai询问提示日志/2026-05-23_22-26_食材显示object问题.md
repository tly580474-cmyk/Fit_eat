# AI 询问提示日志

**日期时间**: 2026-05-23 22:26

## 用户问题
主要食材现在显示的是 "eco [object Object]"，这个也是bug。

## 问题分析
前端代码直接显示 `item` 对象，但现在食材是 `{name, amount}` 格式的对象，导致显示为 `[object Object]`。

## 涉及文件
- `轻饮食软件前端/pages/food-detail.html` - 食物详情页面

## 解决方案
修改食材渲染代码，正确显示 `item.name` 和 `item.amount`。
