# AI 询问提示日志

**日期时间**: 2026-05-23 22:24

## 用户问题
所有菜谱的食材显示都有问题，而且在制作步骤当中，也出现了很多乱码，请审查并修改。

## 问题分析
1. **食材数据格式问题**: 从 HowToCook 导入的食材是纯字符串列表，如 `['巴沙鱼 500g', '蔬菜 300g']`，但前端期望的是 `{name, amount}` 格式的对象列表
2. **步骤中的 Markdown 语法**: 步骤描述中包含了未清理的 Markdown 语法：
   - `[腌制](../../tips/learn/学习腌.md)` - Markdown 链接
   - `**中火**` - Markdown 加粗
   - `![油焖大虾-预览图-1](./油焖大虾.jpg)` - Markdown 图片语法

## 涉及文件
- `轻饮食软件后端/import_howtocook.py` - 食谱导入脚本
- `instance/light_diet.db` - SQLite 数据库

## 解决方案
1. 添加 `parse_ingredient()` 函数，将食材字符串解析为 `{name, amount}` 格式
2. 添加 `clean_markdown()` 函数，清除文本中的 Markdown 语法
3. 更新数据库中 306 条已存在的记录
